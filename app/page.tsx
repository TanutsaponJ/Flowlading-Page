import { Header, Hero, Logo } from "@/components";

const Home = () => {
  return (
    <main className="no-scrollbar">
      <Logo />
      <Header />
      <Hero />
    </main>
  );
};

export default Home;
